export const Configuration = {
  api: {
    base: 'http://localhost:3000',
    // base: 'http://drawserver.ml',
  },
  Refresh: {
    Timeout: 10 * 1000
  },
  Redirection:{
    timeout: 2 * 1000,
  }
}